defaults: &defaults
  working_directory: ~/lcs-img-diff
  docker:
    - image: parity/rust
      environment:
        DISPLAY: ":99"

version: 2
jobs:
  build:
    <<: *defaults
    steps:
      - checkout  
      - run:
          name: Test
          command: |
            npm i
            make build
            npm t   
