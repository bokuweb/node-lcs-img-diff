defaults: &defaults
  working_directory: ~/lcs-img-diff
  docker:
    - image: bokuweb/amazonlinux-neon
      environment:
        DISPLAY: ":99"

version: 2
jobs:
  build:
    <<: *defaults
    steps:
      - checkout  
      - run:
          name: Test
          command: |
            npm i -g yarn
            yarn install
            make build
            yarn test   
